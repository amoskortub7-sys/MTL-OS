# UI mockups

These SVG files are design references for the MTL Flow system:

- `boot-animation.svg` — boot animation concept
- `shutdown-animation.svg` — shutdown animation concept
- `arc-control-center.svg` — right-edge arc control center
- `launcher-flow.svg` — Flow launcher concept

The real flashable image requires a full AOSP GSI environment and arm64 packaging workflow.
